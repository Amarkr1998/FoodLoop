spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/foodloop?currentSchema=notification
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/foodloop
